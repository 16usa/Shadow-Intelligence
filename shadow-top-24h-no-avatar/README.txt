Shadow Intelligence — Top 24H Movers no-avatar v2.5.3

Changes only the 24H mover cards:
- Removes token avatars/images.
- Keeps ticker.
- Shows Market Cap.
- Shows live 24H percentage.
- Keeps the mini 24H sparkline.
- Keeps strongest mover nearest to the bell/right side.
- Keeps 50px bell-row height and 0.5px hairline border.
- Makes cards narrower now that avatars are gone.
- No backend changes and no server restart required.

Install:
cd ~/workspace && rm -rf shadow-top-24h-no-avatar && unzip -o Shadow-Top-24H-No-Avatar-v2.5.3.zip -d shadow-top-24h-no-avatar && bash shadow-top-24h-no-avatar/install.sh
