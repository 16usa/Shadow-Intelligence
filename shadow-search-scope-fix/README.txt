Shadow Intelligence — Search Scope Fix v2.7.2

Required behavior:
- Map / overview page: floating button is visible as magnifier.
- Search page: same button is visible as X.
- Every other page: floating search button is hidden.
- Pressing X completely closes Search, clears its state/results and returns to Map.

Install:
cd ~/workspace && rm -rf shadow-search-scope-fix && unzip -o Shadow-Search-Scope-Fix-v2.7.2.zip -d shadow-search-scope-fix && bash shadow-search-scope-fix/install.sh

No server restart required. Refresh Safari.
