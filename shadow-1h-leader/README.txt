Shadow Intelligence — 1H Fastest Mover v2.5.5

Shows exactly one card next to the bell:
the tracked token with the highest positive growth over the last 1 hour.

Card:
- ticker
- market cap value
- 1H growth %
- 1H sparkline
- no avatar

Refresh:
- server cache 45 sec
- browser refresh 60 sec

Install:
cd ~/workspace && rm -rf shadow-1h-leader && unzip -o Shadow-1H-Fastest-Mover-v2.5.5.zip -d shadow-1h-leader && bash shadow-1h-leader/install.sh

Then restart the main Shadow/Replit app once.
