Shadow Intelligence — Top 24H Movers layout v2.5.1

Changes only the placement and dimensions of the existing 24H Movers strip:
- Moves the strip lower on the Overview/Map page.
- Makes the strip originate from the right/bell side.
- First/top mover appears at the right edge.
- Reduces card height to 62px for a more compact X-like look.
- Reduces token image to 40px and chart to 68x34.
- Preserves the 0.5px hairline border.
- No backend/server changes. No restart required.

Install:
cd ~/workspace && rm -rf shadow-top-24h-layout && unzip -o Shadow-Top-24H-Layout-v2.5.1.zip -d shadow-top-24h-layout && bash shadow-top-24h-layout/install.sh
