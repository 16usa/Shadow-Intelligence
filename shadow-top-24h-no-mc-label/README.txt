Shadow Intelligence — Top 24H Movers v2.5.4

Small UI cleanup:
- Removes only the literal "MC" label from each mover card.
- Keeps the market-cap amount itself, e.g. $8.2M.
- Keeps ticker, 24H growth percentage, sparkline, ordering, bell-row layout.
- No server restart required.

Install:
cd ~/workspace && rm -rf shadow-top-24h-no-mc-label && unzip -o Shadow-Top-24H-No-MC-Label-v2.5.4.zip -d shadow-top-24h-no-mc-label && bash shadow-top-24h-no-mc-label/install.sh
